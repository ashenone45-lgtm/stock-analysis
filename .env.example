# 飞书机器人 Webhook（推荐）
# 在飞书群 -> 群设置 -> 机器人 -> 添加机器人 -> 自定义机器人 中获取
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/your-token-here

# 钉钉机器人 Webhook（备选）
# 在钉钉群 -> 群设置 -> 智能群助手 -> 添加机器人 -> 自定义 中获取
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=your-token-here

# 注意：
# 1. 将此文件复制为 .env 并填入真实的 Webhook URL
# 2. 只需填写其中一个，两个都填则同时推送
# 3. .env 文件不会被提交到 git（已加入 .gitignore）
